% Fig — the three independent cross-checks converging on the per-site result.
% Mirrors sec:crosschecks (Diviner closure, Martinez density model,
% robustness battery). Body only; the book supplies \caption/\label.
\begin{tikzpicture}[
  font=\footnotesize,
  every node/.style={align=center},
  io/.style={rectangle, rounded corners=10pt, draw=dim, line width=0.9pt,
             fill=black!3, inner sep=5pt, text width=84mm},
  chk/.style={rectangle, rounded corners=2pt, draw=teal, line width=0.9pt,
              fill=teal!7, inner sep=5pt, text width=38mm},
  res/.style={rectangle, rounded corners=3pt, draw=forest, fill=forest!12,
              line width=1pt, inner sep=5pt, text width=86mm},
  arr/.style={-{Stealth[length=2.2mm]}, line width=0.9pt, draw=dim,
              rounded corners=3pt, line cap=round}]

  \node[io] (kd) at (0,0)
    {\textbf{the per-site retrieval} --- $K_d^{*}=4.60$ / $7.08$
     mW\,m$^{-1}$\,K$^{-1}$\\
     {\scriptsize\color{dim}fits the 7 + 16 deep thermometers;
      could it be fooled by mis-specified surroundings?}};

  \node[chk] (c1) at (-4.8,-2.4)
    {\textbf{Diviner surface closure}\\
     {\scriptsize the same $K_d^{*}$ reproduces LRO's orbital
      \emph{surface} swing to $\sim$4--6\,K}\\[1.5pt]
     {\scriptsize\itshape\color{dim}kills: a broken forcing/albedo chain}};
  \node[chk] (c2) at (0,-2.4)
    {\textbf{Mart\'inez density model}\\
     {\scriptsize an independent $K(T,\rho)$ form confirms the A17
      excess is not bulk density}\\[1.5pt]
     {\scriptsize\itshape\color{dim}kills: a form-specific artifact}};
  \node[chk] (c3) at (4.8,-2.4)
    {\textbf{robustness battery}\\
     {\scriptsize slide the 80\,cm cut, the window threshold,
      $\pm$2\,K offsets: $K_d^{*}$ moves $<$ its CI}\\[1.5pt]
     {\scriptsize\itshape\color{dim}kills: a load-bearing choice}};

  \node[res] (ok) at (0,-4.8)
    {\textbf{three disjoint slices of physics, one answer} --- no single
     sensor, model form, or discretionary cut carries the result};

  \draw[arr] (kd.south) -- (c2.north);
  \draw[arr] (kd.south) -- ($(kd.south)+(0,-4.5mm)$) -| (c1.north);
  \draw[arr] (kd.south) -- ($(kd.south)+(0,-4.5mm)$) -| (c3.north);
  \draw[arr] (c1.south) -- ($(c1.south)+(0,-4.5mm)$) -| ($(ok.north)+(-2.7,0)$);
  \draw[arr] (c2.south) -- (ok.north);
  \draw[arr] (c3.south) -- ($(c3.south)+(0,-4.5mm)$) -| ($(ok.north)+(2.7,0)$);
\end{tikzpicture}
