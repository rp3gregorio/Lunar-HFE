% Standalone wrapper for the flux-anchored loop figure — fast iteration.
% Compile:   pdflatex fluxanchored_standalone.tex
\documentclass[border=4mm]{standalone}
\input{_preamble}
\begin{document}
\input{fluxanchored}
\end{document}
