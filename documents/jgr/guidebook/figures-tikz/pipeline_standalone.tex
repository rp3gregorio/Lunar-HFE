% Standalone wrapper for Fig 1.4 — fast iteration.
% Compile:   pdflatex pipeline_standalone.tex
% Output:    pipeline_standalone.pdf  (one tightly-cropped page)
\documentclass[border=4mm]{standalone}
\input{_preamble}
\begin{document}
\input{pipeline}
\end{document}
