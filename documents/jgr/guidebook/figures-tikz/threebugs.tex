% Fig — the three inputs that bit us: input -> symptom -> fix -> effect on
% K_d*(A17). Values from the audit record. Body only.
\begin{tikzpicture}[font=\scriptsize, every node/.style={align=center},
  inp/.style={rectangle, rounded corners=2pt, draw=coral, fill=coral!8,
              line width=0.9pt, inner sep=4pt, text width=30mm},
  sym/.style={rectangle, rounded corners=2pt, draw=dim, fill=white,
              line width=0.8pt, inner sep=4pt, text width=40mm},
  fixs/.style={rectangle, rounded corners=2pt, draw=forest, fill=forest!9,
              line width=0.9pt, inner sep=4pt, text width=40mm},
  eff/.style={font=\scriptsize\bfseries\color{char}, text width=22mm},
  arr/.style={-{Stealth[length=2mm]}, line width=0.8pt, draw=dim, line cap=round}]
  \foreach \y/\A/\B/\C/\D [count=\r] in {%
    0/{$Q_b$(A17) $=15$\\{\tiny a non-borehole value, inherited silently}}/%
      {both sites biased; contrast inflated}/%
      {16 mW m$^{-2}$ (Langseth borehole value); one source of truth in \code{config}}/%
      {$8.12 \to 7.51$},%
    -1.75/{$n_{\rm inner}=12$\\{\tiny ``the skin settles fast''}}/%
      {$K_d^{*}$ still drifting with the setting itself}/%
      {truncated Step A $+$ $n_{\rm inner}=96$, certified by the $K_d^{*}$ plateau}/%
      {$7.51 \to 7.16$},%
    -3.5/{$\Delta t=3600$ s\\{\tiny + a march that skipped the wrap step}}/%
      {periodic state first-order in $\Delta t$; depth-uniform bias}/%
      {1800 s, wrap step, midpoint properties; dt ladder $+$ Richardson check}/%
      {$7.16 \to 7.08$}}
  {
    \node[inp]  (a\r) at (0,\y)    {\A};
    \node[sym]  (b\r) at (4.3,\y)  {\textbf{symptom:} \B};
    \node[fixs] (c\r) at (9.3,\y)  {\textbf{fix:} \C};
    \node[eff]  (d\r) at (13.2,\y) {$K_d^{*}$(A17)\\ \D};
    \draw[arr] (a\r) -- (b\r); \draw[arr] (b\r) -- (c\r); \draw[arr] (c\r) -- (d\r);
  }
  \node[font=\scriptsize\itshape\color{dim}, anchor=north, text width=125mm]
    at (6.6,-4.55) {the meta-rule: a value that ``looks settled'' earns a
    re-derivation, not a pass --- and every move is recorded, which is what
    makes the final number credible};
\end{tikzpicture}
