% Standalone wrapper for the "why the deep column is (almost) free" slide figure.
% Compile:  pdflatex deepprofile_slide_standalone.tex  (or ./build.sh deepprofile_slide)
\documentclass[border=6mm]{standalone}
\input{_preamble}
\begin{document}
\input{deepprofile_slide}
\end{document}
