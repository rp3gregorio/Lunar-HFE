% Fig — how an input earns its register row: the four evidence classes.
% Mirrors the register table of Ch.5. Body only.
\begin{tikzpicture}[font=\footnotesize, every node/.style={align=center},
  io/.style={rectangle, rounded corners=10pt, draw=dim, fill=black!3,
             line width=0.9pt, inner sep=5pt, text width=64mm},
  q/.style={rectangle, rounded corners=2pt, draw=teal, fill=teal!7,
            line width=0.9pt, inner sep=4pt, text width=34mm},
  cls/.style={rectangle, rounded corners=3pt, line width=1pt, inner sep=4pt,
              text width=34mm, font=\scriptsize},
  arr/.style={-{Stealth[length=2.2mm]}, line width=0.9pt, draw=dim,
              rounded corners=3pt, line cap=round}]
  \node[io] (in) at (0,0)
    {\textbf{a number wants into the model}\\
     {\scriptsize\color{dim}$Q_b$, albedo, $\Delta t$, $z_0$, $n_{\rm inner}$, the 80-cm cut, \ldots}};
  \node[q] (q1) at (-5.7,-2.1) {published\\ \textbf{measurement}?};
  \node[q] (q2) at (-1.9,-2.1) {numerical\\ \textbf{knob}?};
  \node[q] (q3) at (1.9,-2.1)  {discretionary\\ \textbf{choice}?};
  \node[q] (q4) at (5.7,-2.1)  {\textbf{derived} from\\ the physics?};
  \node[cls, draw=forest, fill=forest!9]  (c1) at (-5.7,-4.15)
    {cite the source; carry its \textbf{envelope} into the error budget\\
     {\tiny ($Q_b$, albedo, $K_s$, $\rho_d$)}};
  \node[cls, draw=teal, fill=teal!8]      (c2) at (-1.9,-4.15)
    {\textbf{convergence-tested}: show $K_d^{*}$ stopped moving\\
     {\tiny ($\Delta t$, $n_{\rm inner}$, grid, $K_d$-grid)}};
  \node[cls, draw=coral, fill=coral!8]    (c3) at (1.9,-4.15)
    {\textbf{sensitivity row}: sweep it, report the half-range\\
     {\tiny (80-cm cut, stability threshold)}};
  \node[cls, draw=teal!60!black, fill=teal!5]      (c4) at (5.7,-4.15)
    {\textbf{derivation note} in the register\\
     {\tiny ($z_0$ below the rectification zone)}};
  \draw[arr] (in.south) -- ($(in.south)+(0,-4mm)$) -| (q1.north);
  \draw[arr] (in.south) -- ($(in.south)+(0,-4mm)$) -| (q2.north);
  \draw[arr] (in.south) -- ($(in.south)+(0,-4mm)$) -| (q3.north);
  \draw[arr] (in.south) -- ($(in.south)+(0,-4mm)$) -| (q4.north);
  \foreach \i in {1,2,3,4} \draw[arr] (q\i) -- (c\i);
\end{tikzpicture}
