% Fig — reproducing the result: the four make targets and what each writes.
% Mirrors the Makefile (retrieve -> aux -> figures -> paper = make all).
% Body only; the book supplies \caption/\label.
\begin{tikzpicture}[
  font=\footnotesize,
  every node/.style={align=center},
  tgt/.style={rectangle, rounded corners=2pt, draw=forest, line width=0.9pt,
              fill=forest!8, inner sep=5pt, text width=30mm, minimum height=13mm},
  art/.style={font=\scriptsize\color{dim}, text width=32mm},
  arr/.style={-{Stealth[length=2.2mm]}, line width=0.9pt, draw=dim, line cap=round}]

  \node[tgt] (t1) at (0,0)
    {\code{make retrieve}\\{\scriptsize sweep + bootstrap}};
  \node[tgt, right=7mm of t1] (t2)
    {\code{make aux}\\{\scriptsize sensitivities, budget,\\MCMC, Diviner}};
  \node[tgt, right=7mm of t2] (t3)
    {\code{make figures}\\{\scriptsize every generator\\in \code{JOBS}}};
  \node[tgt, right=7mm of t3] (t4)
    {\code{make paper}\\{\scriptsize letter +\\guidebook PDFs}};

  \node[art, below=2mm of t1] {$\rightarrow$ \code{kd\_retrieval\_\\results.json}};
  \node[art, below=2mm of t2] {$\rightarrow$ ten more\\\code{results/*.json}};
  \node[art, below=2mm of t3] {$\rightarrow$ \code{results/\\figures/*.pdf}};
  \node[art, below=2mm of t4] {$\rightarrow$ the documents\\quote all of it};

  \draw[arr] (t1) -- (t2);
  \draw[arr] (t2) -- (t3);
  \draw[arr] (t3) -- (t4);

  \node[font=\scriptsize\itshape\color{dim}, above=3mm of t2, xshift=11mm]
    {\code{make all} runs the whole chain; \code{make test} guards it (49 tests)};
\end{tikzpicture}
