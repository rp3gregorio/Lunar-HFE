% Fig — certification: three independent checks, each killing one failure mode.
% Mirrors equilibrium._mean_flux_closure + the §cert table (0.05 K / 10% / 0.08 K).
% Body only; the book supplies \caption/\label.
\begin{tikzpicture}[
  font=\footnotesize,
  every node/.style={align=center},
  io/.style={rectangle, rounded corners=10pt, draw=dim, line width=0.9pt,
             fill=black!3, inner sep=5pt},
  chk/.style={rectangle, rounded corners=2pt, draw=teal, line width=0.9pt,
              fill=teal!7, inner sep=5pt, text width=37mm},
  ok/.style={rectangle, rounded corners=3pt, draw=forest, fill=forest!12,
             line width=1pt, inner sep=5pt, text width=86mm},
  arr/.style={-{Stealth[length=2.2mm]}, line width=0.9pt, draw=dim,
              rounded corners=3pt, line cap=round}]

  \node[io, text width=88mm] (solve) at (0,0)
    {\textbf{a converged solve} --- the loop says the anchor stopped moving\\
     {\scriptsize\color{dim}necessary, but the F1 bug taught us it is not sufficient}};

  \node[chk] (c2) at (0,-2.2)
    {\textbf{flux closure}\\ $|K\,d\Tm/dz - Q_b|/Q_b$\\ at 0.55, 1.5, 3.0 m $\leq 10\%$\\[1.5pt]
     {\scriptsize\itshape\color{dim}kills: an inconsistent Step~B}};
  \node[chk] (c1) at (-4.6,-2.2)
    {\textbf{guess independence}\\ re-solve from 240 K:\\ $\max_z|\Delta T| \leq 0.05$ K\\[1.5pt]
     {\scriptsize\itshape\color{dim}kills: fixed point of ``eqns $+$ guess''}};
  \node[chk] (c3) at (4.6,-2.2)
    {\textbf{honest re-run}\\ 120 brute lunations from\\ the answer: drift $\leq 0.08$ K\\[1.5pt]
     {\scriptsize\itshape\color{dim}kills: a near-miss brute would expose}};

  \node[ok] (cert) at (0,-4.7)
    {\textbf{certified periodic steady state} --- only now does the profile
     enter the $K_d$ sweep};

  \draw[arr] (solve.south) -- (c2.north);
  \draw[arr] (solve.south) -- ($(solve.south)+(0,-4.5mm)$) -| (c1.north);
  \draw[arr] (solve.south) -- ($(solve.south)+(0,-4.5mm)$) -| (c3.north);
  \draw[arr] (c1.south) -- ($(c1.south)+(0,-4.5mm)$) -| ($(cert.north)+(-2.6,0)$);
  \draw[arr] (c2.south) -- (cert.north);
  \draw[arr] (c3.south) -- ($(c3.south)+(0,-4.5mm)$) -| ($(cert.north)+(2.6,0)$);
\end{tikzpicture}
